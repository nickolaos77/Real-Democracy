{{#section 'Menu'}}
    {{!-- This is a section used inside the current view which will be rendered inside the main layout         Chapter 7  OReilly.Web.Development.With.Node.And.Express--}}

<div id="navbar" class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
        <li class="a_ctive"><a href="/">All</a></li>
        <li><a href="/myPics">My Pics</a></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Add a Pic <span class="caret"></span></a>
            <ul class="dropdown-menu">

                <form method="POST"  action='/myPics'>
                    <div class="form-group">
                        <input type="text" class="form-control" id="url" placeholder="Pic url..." name='imageUrl'>
                    </div>
                    <div class="form-group">
                        <input type="text" maxlength="20" class="form-control" id="description" placeholder="Pic description..." name='description'>
                    </div>
                    <div class="form-group">
                        <button type="submit" class='btn btn-primary'> Submit </button>
                    </div>
                </form>
            </ul>
        </li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
        <li>
            <a href="/logout" style='padding-top:5px; padding-bottom:3px;'>
                <button type="button" class="btn btn-default navbar-btn">Logout</button>
            </a>
        </li>
    </ul>
</div>

{{/section}}

{{> imageUser}} {{!-- goes to the body of the layout --}}
